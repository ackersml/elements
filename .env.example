# Production site URL (no trailing slash) — used for metadata, sitemap, Stripe success URLs
NEXT_PUBLIC_APP_URL=https://your-deployment.vercel.app

# Optional: short looped audio for hero (leave unset to hide the gate copy)
# NEXT_PUBLIC_HERO_AUDIO_URL=

# Which checkout the buy buttons use: "shopify" or "stripe" (default: stripe).
# NEXT_PUBLIC_CHECKOUT_PROVIDER=shopify

# Shopify Storefront API (server — catalogue lives in Shopify, checkout is hosted there).
# The Storefront access token is a PUBLIC token (safe to expose); the Admin API
# token is not — don't put an Admin token here.
# Create via: Shopify admin ▸ Settings ▸ Apps and sales channels ▸ Develop apps.
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_STOREFRONT_ACCESS_TOKEN=YOUR_STOREFRONT_ACCESS_TOKEN_HERE
# Optional — pin the Storefront API version (default: 2025-10)
# SHOPIFY_STOREFRONT_API_VERSION=2025-10

# Stripe (server) — legacy checkout, used when NEXT_PUBLIC_CHECKOUT_PROVIDER is not "shopify"
STRIPE_SECRET_KEY=sk_live_or_test_YOUR_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# Supabase (server — orders + webhooks)
# Vercel ↔ Supabase integration usually sets NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY.
# Manual setup can use SUPABASE_URL instead of NEXT_PUBLIC_SUPABASE_URL.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE

# Fulfillment stub (optional)
# FULFILLMENT_MODE=manual
